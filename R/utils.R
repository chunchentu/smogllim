library(Rcpp)
# R_logsumexp = function(x, axis=1){
#     # the summation along the axis
#     # axis is 0-indexed
#     # axis = 0: sum along rows
#     # axis = 1: sum along columns
#
#     s = cpp_logsumexp(x, axis)
# }
